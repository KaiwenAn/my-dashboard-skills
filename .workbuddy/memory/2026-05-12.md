# 2026-05-12 日志

## 重大修复：describe_table 和 fetch_results 方法

### 问题
用户期望的 skill 用法是：用户通过自然语言输入表名后，skill 能自动获取表详情（字段名、类型、注释），然后自主判断需要哪些字段。

但 `describe_table` 方法一直无法获取到字段信息。

### 根因分析
经过深入调试，发现有两个问题：

1. **`fetch_results` 方法的请求格式错误**：
   - 原代码：`resp = self.session.post(url, json=payload, headers=...)`
   - 问题：queryId 作为 JSON body 传递，导致 404 错误
   - 修复：改为 `resp = self.session.post(url, params=payload, headers=...)`，queryId 作为 URL 参数传递

2. **`execute_query` 对非 SELECT/WITH 语句不获取结果**：
   - 原代码：`if sql.strip().upper().startswith("SELECT") or sql.strip().upper().startswith("WITH")`
   - 问题：`DESCRIBE` 语句不以 "SELECT" 开头，所以不调用 `fetch_results`
   - 修复：在 `describe_table` 中直接调用底层方法（`submit_query` + `wait_for_completion` + `fetch_results`）

### 修改文件
- `C:\Users\Kai\WorkBuddy\20260427134240\data_platform_api.py`

### 修复后的效果
- `describe_table` 方法能正确获取表字段信息
- 测试表：`iceberg_zjyprc_hadoop.meta.dwd_user_module_page_view`
- 获取到 18 个字段，包括：date, uid, e_ts, session_id, module, sub_module, page_name 等

### 关键 API 端点
- 提交查询：`POST /olap/api/v2/statement/query`
- 轮询状态：`POST /olap/api/v2/statement/getStatusAndLog`（params 参数）
- 获取结果：`POST /olap/api/v2/statement/fetchResult`（**params 参数，不是 json body**）
- 关闭查询：`POST /olap/api/v2/statement/close`（params 参数）

---

## 新增：自然语言输入转换层

### 新增文件
- `scripts/nl_converter.py` - 自然语言转结构化JSON转换器

### 修改文件
- `scripts/run_pipeline.py` - 增加 `--natural-input` 和 `--natural-input-file` 参数
- `SKILL.md` - 添加自然语言输入方式说明

### 使用方式
```bash
# 命令行直接输入
python run_pipeline.py --natural-input "帮我做一个用户行为分析看板，数据源是iceberg_zjyprc_hadoop.meta.dwd_user_module_page_view"

# 从文件读取
python run_pipeline.py --natural-input-file input.txt --output ./output
```

### NLConverter 能力
- 提取看板标题（LLM）
- 识别数据源表名（正则+LLM）
- 自动调用 describe_table 获取字段信息
- 推断常见指标和维度（LLM+规则）
- 生成 confirmation_items 确认清单

---

## 路径问题修复（2026-05-12 下午）

### 问题
用户报告指出多个文件存在硬编码路径问题，导致在错误目录执行命令时报错。

### 修复文件
1. **scripts/run_pipeline.py 第38行**
   - 原：`_default_workspace = str(Path.home() / "WorkBuddy" / "20260427134240")`
   - 改：`_default_workspace = str(SKILL_DIR.parent)`
   - 效果：自动检测脚本所在目录的父目录作为默认工作空间

2. **USAGE.md 第32-35行**
   - 原路径：`c:\Users\Kai\WorkBuddy\20260427134240`（错误目录）
   - 原命令：`python main.py`（文件不存在）
   - 改路径：`c:\Users\Kai\.workbuddy\skills\dashboard-agent\my-dashboard-skills`
   - 改命令：`python scripts/run_pipeline.py`

3. **INSTALL.md 第52行**
   - 原：硬编码 `c:\Users\Kai\WorkBuddy\20260427134240\`
   - 改：通用说明 + 示例格式 `# 例如：c:\Users\YourName\WorkBuddy\20260427134240\`

4. **scripts/temp_describe.py 第4行**
   - 原：`sys.path.insert(0, r'C:\Users\Kai\WorkBuddy\20260427134240')`
   - 改：自动检测 + 环境变量 `WORKSPACE_DIR` 覆盖

### 验证
修复后搜索 `C:\Users\Kai\WorkBuddy\20260427134240` 模式，仅剩 USAGE.md 第32行的正确路径。

### 待优化
- `temp_describe.py` 中仍硬编码了 `token` 和 `base_url`，建议参数化（未修复）

---

## 核心模块缺失修复（2026-05-12 傍晚）

### 问题
用户在 `my-dashboard-skills` 目录执行 `python scripts/run_pipeline.py` 时报错：`No module named 'llm'`

### 根因
`my-dashboard-skills` 目录缺少核心 Python 模块：
- `llm.py`、`pipeline.py`、`agents.py`、`bi_api.py`、`data_platform_api.py`、`renderer.py`
- 这些模块在 `C:\Users\Kai\WorkBuddy\20260427134240\` 工作空间，但不在 skill 目录中
- `run_pipeline.py` 的导入逻辑假设这些模块在 `scripts/` 的父目录（即项目根目录）

### 修复方案
**方案1（已执行）**：将核心模块复制到 skill 目录，使 skill 自包含

### 已复制的文件

**核心模块（到 `my-dashboard-skills\` 根目录）**：
- ✅ `agents.py` (41KB)
- ✅ `pipeline.py` (25KB)
- ✅ `llm.py` (10KB)
- ✅ `bi_api.py` (22KB)
- ✅ `data_platform_api.py` (25KB)
- ✅ `renderer.py`

**配置和提示词**：
- ✅ `scripts/config.json` - 配置文件
- ✅ `prompts/` - Agent system prompt 目录（6个 .md 文件）

### 验证结果
```bash
cd c:\Users\Kai\.workbuddy\skills\dashboard-agent\my-dashboard-skills
python -c "from llm import LLMClient; from pipeline import Pipeline; from agents import RunMode; print('✅ 所有模块导入成功！')"
```
✅ 输出：`llm OK`、`pipeline OK`、`agents OK`、所有核心模块导入成功！

```bash
python scripts/run_pipeline.py --help
```
✅ 成功显示帮助信息，所有参数说明正常！

### 经验总结
1. **Skill 目录应该自包含**：所有依赖的 Python 模块应该放在 skill 目录内，不应依赖外部工作空间
2. **复制顺序**：按导入依赖顺序复制（`llm.py` → `pipeline.py` → `agents.py` → `bi_api.py` → `data_platform_api.py` → `renderer.py`）
3. **别忘了 prompts/ 目录**：Agent 的 system prompt 文件是必须的，否则运行时会出现文件找不到错误
4. **测试导入而非运行**：先用 `python -c "import ..."` 测试模块导入，再测试完整运行，更快定位问题

### 修复后的目录结构
```
my-dashboard-skills/
├── agents.py              ✅ 核心模块
├── pipeline.py            ✅ 核心模块
├── llm.py                ✅ 核心模块
├── bi_api.py             ✅ 核心模块
├── data_platform_api.py   ✅ 核心模块
├── renderer.py            ✅ 核心模块
├── prompts/              ✅ Agent prompts
│   ├── requirements-parser-agent.md
│   ├── semantic-model-agent.md
│   ├── chart-design-agent.md
│   ├── instruction-generator-agent.md
│   ├── solution-generator-agent.md
│   └── bi-push-agent.md
├── scripts/
│   ├── run_pipeline.py   ✅ 入口脚本
│   ├── nl_converter.py   ✅ 自然语言转换
│   ├── temp_describe.py  ✅ 临时测试脚本
│   └── config.json       ✅ 配置文件
├── references/           ✅ 参考文档
├── USAGE.md              ✅ 使用指南
└── SKILL.md              ✅ Skill 定义
```

### 后续建议
- [ ] 将这些核心模块提交到 Git，确保 skill 完整性
- [ ] 更新 `SKILL.md` 和 `USAGE.md`，明确说明 skill 目录结构
- [ ] 考虑在 `run_pipeline.py` 中添加自检逻辑，启动时检查核心模块是否存在
